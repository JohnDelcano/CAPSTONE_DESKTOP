import React from "react";
import ReactDOM from "react-dom/client";
import SecondWindow from "./second/pages/secondWindow";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SecondWindow />
  </React.StrictMode>
);
